# Copy this file to .env.fly, fill in the values, then run:
#   fly secrets import < .env.fly
#
# Never commit .env.fly — it contains real secrets.

# ── Database ──────────────────────────────────────────────────────────────────
# Points to the persistent Fly volume mounted at /data
DATABASE_URL=file:/data/prod.db

# ── Auth ──────────────────────────────────────────────────────────────────────
# Generate with: openssl rand -base64 32
AUTH_SECRET=replace-with-output-of-openssl-rand-base64-32

# The public URL of your deployed app (no trailing slash)
NEXTAUTH_URL=https://your-app-name.fly.dev

# ── OpenAI ────────────────────────────────────────────────────────────────────
OPENAI_API_KEY=sk-proj-...
