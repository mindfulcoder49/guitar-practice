# Copy to .env.local and fill in values.
# Next.js loads .env.local automatically; it overrides .env.
# Do not commit .env.local — it contains your auth secret.

# ── Database ──────────────────────────────────────────────────────────────────
# Matches the value in .env — kept here so Next.js server routes can read it.
DATABASE_URL="file:./dev.db"

# ── Auth ──────────────────────────────────────────────────────────────────────
# Any random string works for local dev. Generate a real one for production:
#   openssl rand -base64 32
AUTH_SECRET="local-dev-secret-change-in-production"

# Full URL of the app (no trailing slash)
NEXTAUTH_URL="http://localhost:3000"
